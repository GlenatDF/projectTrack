// Re-export shim — all types now live in src/shared/lib/types/.
// This file is kept so all existing import paths (e.g. '../lib/types') continue to work unchanged.
export * from '../shared/lib/types';
