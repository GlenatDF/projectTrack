// Re-export shim — all API wrappers now live in src/shared/lib/api/.
// This file is kept so all existing import paths (e.g. '../lib/api') continue to work unchanged.
export * from '../shared/lib/api';
